appId: com.zcord.desktop
productName: zcord
files:
  - dist/**
  - dist-electron/**
  - package.json
asar: true
directories:
  output: release
publish: null
win:
  target:
    - nsis
  artifactName: "zcord-Setup-${version}.${ext}"
mac:
  target:
    - dmg
linux:
  target:
    - AppImage
