appId: com.rucord.desktop
productName: Rucord
files:
  - dist/**
  - dist-electron/**
  - package.json
asar: true
directories:
  output: release
publish: null
win:
  target:
    - nsis
  artifactName: "Rucord-Setup-${version}.${ext}"
mac:
  target:
    - dmg
linux:
  target:
    - AppImage
