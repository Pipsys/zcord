# certs/

Local certificates directory (never commit private certs/keys).

Use this directory only for local development certificates if required.
Keep production certificates in secure storage (vault/secret manager), not in Git.
